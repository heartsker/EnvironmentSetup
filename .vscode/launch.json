{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "target": "./bin/executable",
            "name": "Debug",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": ["4"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build C++ code"
        }
    ]
}